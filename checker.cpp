#include<bits/stdc++.h>

using namespace std;
int main(int argc, char *argv[]){
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
  mt19937_64 rd(atoi(argv[1]));
  #define rd(x) rd() % (x)
  /*
  Please put generate code here.
  If you want to get a number which is greater than or equal x ans less than b,please use rd(b) + a 
  */
  return 0;
}
